# 2026-04-22

## Golf Scorecard Automation — Hickory Hills

### Session Summary
Worked with Josh all day on the Hickory Hills Wednesday Night Men's League scorecard generator.

### Discord Bot Setup
- Josh configured a Discord bot using a bot token (`MTQ5NjU5MzczMTYxMTEzMjIwNg...`) and server ID `1306707942300454932`
- User ID: `324726656067567617`
- `groupPolicy: open` — bot responds in all server channels
- Bot is working for DMs but NOT for server channels — needed Message Content Intent enabled in Discord Developer Portal (bot has connect permission but can't read message content without it)

### Scorecard Generator — Final State
- **Script**: `/home/thecarter34/.openclaw/workspace/development/GolfLeague/scorecard_automation/generate_scorecards.py`
- **Input**: Drop PDF + CSV in `input/`, Josh tells me to process, I parse → write `data.json` → run script
- **Output**: `output/Week_N_print.html` — print-ready scorecards, green/gold on screen, black on print

### Scorecard Format
- 9-hole HDCP = `round(18h_full / 2)` from CSV Players section
- Stroke dots (●) from PDF-extracted stroke allocation per player
- Tee time hardcoded to **5:30 PM** for all groups
- Bottom summary section added with 6 columns:
  - Team (last names of 2 players, formatted as "Last1/Last2")
  - Total Score | Hndcp | Net Score | Team Pts | Hole Pts | Total Pts
  - Team column: 62pt wide; data columns equal size (~65pt), total width matches scorecard table
- Last names dynamically extracted from full names

### Workflow (Manual for Now)
1. Josh drops PDF in `input/`
2. Josh tells me to process
3. I parse PDF → write `data.json` → run `generate_scorecards.py`
4. Output HTML written to `output/Week_N_print.html`
5. Input files cleaned up (PDF + data.json deleted after processing)

### Files Created/Modified
- `generate_scorecards.py` — rebuilt as PDF+CSV hybrid with summary table
- `Week_2_data.json` in `input/` — mock data for testing (13 groups, all players)
- `output/Week_2_print.html` — generated test output
- `input/` cleaned of PDF + data.json after processing

### Discord Troubleshooting
- Bot working in DMs, not in server channels
- Fix: Enable **Message Content Intent** + **Server Members Intent** in Discord Developer Portal → Bot → Privileged Gateway Intents
- Config patch needed after enabling intents: `channels.discord.guilds.<id>.groupPolicy: "open"`

### Next Week (Week 3)
- Josh drops new PDF → I parse → write `data.json` → run generator
- Same workflow, no cron trigger (Josh removed it, found alerts annoying)